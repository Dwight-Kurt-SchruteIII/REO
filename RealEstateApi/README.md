# RealEstateApi

Demo projekt - správa nemovitostí, nájemních smluv a plateb.

.NET 8 Minimal API + EF Core + SQL Server v Dockeru.

## Spuštění

```bash
cp .env.example .env
docker-compose up -d
dotnet run
```

Swagger UI: `http://localhost:5062/swagger`